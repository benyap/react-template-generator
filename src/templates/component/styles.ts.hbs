import { makeStyles, createStyles, Theme } from "@material-ui/core/styles";
import { {{ properCase name }}Props } from "./types";

export default makeStyles<Theme, {{ properCase name }}Props>(theme =>
  createStyles({
    root: {}
  })
);
