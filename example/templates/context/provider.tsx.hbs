import React, { FC } from "react";

import {{ properCase name }}Context from ".";

/**
 * TODO: add description
 */
export const {{ properCase name }}Provider: FC = props => {
  return (
    <{{ properCase name }}Context.Provider value=\{{}}>
      {props.children}
    </{{ properCase name }}Context.Provider>
  );
};
